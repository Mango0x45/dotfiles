#!/bin/sh

stdbuf -oL -- tiktok                 \
	'%A, %-d %B %Y — %T %Z'          \
	TZ=Europe/Helsinki ' (%H:%M %Z,' \
	TZ=Pacific/Auckland ' %H:%M %Z)' \
| while read line
do
	echo "$line" | ordinal -p '^\w+, (\d+)'
done | sed 's/^./\U&/g'
