#!/bin/sh

# TODO: -s flag to apropos

page="$(apropos . \
	| sed -E 's/ (\([^)]+\))\s*-/\1 —/' \
	| sort \
	| osel \
	| sed 's/\s.*//')"

if [ -n "$page" -a ! -t 2 ]
then
	man -Tpdf "$page" | zathura -
elif [ -n "$page" -a -t 2 ]
then
	man "$page"
fi
